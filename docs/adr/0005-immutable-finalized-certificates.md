# Immutable finalized certificates

**Status: accepted**

A finalized certificate cannot be edited. If a correction is needed, the system creates a new certificate and new valve snapshot for the same Válvula, preserving the prior certificate as historical evidence. This keeps the signed data and the data presented for each certification event stable and auditable.
