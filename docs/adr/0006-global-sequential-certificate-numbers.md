# Global sequential certificate numbers

**Status: accepted**

Certificate numbers are generated by the database from one global transactional counter when a certificate is finalized. The counter is locked and incremented in the same transaction that records finalization, so concurrent finalizations cannot receive duplicate numbers and the identifier is shared across all Talleres Móviles, Válvulas, Yacimientos, and Técnicos.
